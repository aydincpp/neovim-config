-- lua/plugins/plenary.lua
--
-- Plenary.nvim is a utility library providing Lua functions
-- for other plugins. This plugin is loaded eagerly (no lazy-loading)
-- because many plugins depend on it and require it to be available
-- immediately when they load.

return {
  "nvim-lua/plenary.nvim",
}
